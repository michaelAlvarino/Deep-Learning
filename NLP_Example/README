Make sure you have completed all the requirements in HW3 Prerequisties.
Use "tar -xzf aclImdb.tar.gz" to extract the reviews.
If you haven't downloaded the Glove Embeddings, Use <wget http://nlp.stanford.edu/data/glove.42B.300d.zip> to get the pretrained word Embeddings, Extract them using unzip.
Submit only the hw3.ipynb with all the saved outputs as your Homework Solutions. The submission should be in the form of "hw3/hw3.ipynb"
